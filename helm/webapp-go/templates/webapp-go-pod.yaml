apiVersion: v1
kind: Pod
metadata:
  name: {{ template "fullname" . }}
  labels:
    name: {{ template "fullname" . }}
    component: webapp-go
spec:
  containers:
  - name: {{ template "fullname" . }}
    image: "webapp-go:latest"
    imagePullPolicy: Never